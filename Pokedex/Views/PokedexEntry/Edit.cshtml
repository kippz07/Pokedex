@model Pokedex.ViewModels.EntryEditViewModel
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Customise your @Model.Entry.Name</h2>

@using (Html.BeginForm("Save", "PokedexEntry"))
{
    <div class="form-group">
        @Html.LabelFor(p => p.Entry.Nickname)
        @Html.TextBoxFor(p => p.Entry.Nickname)
    </div>

    <div class="form-group">
        @Html.LabelFor(p => p.Entry.Move1)
        @Html.DropDownListFor(p => p.Entry.Move1, new SelectList(Model.Pokemon.Moves, "Name", "Name"), "Select Move")
    </div>

    <div class="form-group">
        @Html.LabelFor(p => p.Entry.Move2)
        @Html.DropDownListFor(p => p.Entry.Move2, new SelectList(Model.Pokemon.Moves, "Name", "Name"), "Select Move")
    </div>

    <div class="form-group">
        @Html.LabelFor(p => p.Entry.Move3)
        @Html.DropDownListFor(p => p.Entry.Move3, new SelectList(Model.Pokemon.Moves, "Name", "Name"), "Select Move")
    </div>

    <div class="form-group">
        @Html.LabelFor(p => p.Entry.Move4)
        @Html.DropDownListFor(p => p.Entry.Move4, new SelectList(Model.Pokemon.Moves, "Name", "Name"), "Select Move")
    </div>
    @Html.HiddenFor(p => p.Entry.Id);
    @Html.HiddenFor(p => p.Entry.PokemonId);
    <button type="submit" class="btn btn-primary">Save</button>
    @Html.ActionLink("Return to Pokemon", "Index", "Pokemon", new { gen = 1, page = 0 }, new { @class = "btn btn-danger" });
}